[package]
name = "tank-postgres"
description = "Postgres driver implementation for Tank: the Rust data layer"
version.workspace = true
authors.workspace = true
license.workspace = true
edition.workspace = true
repository.workspace = true
categories.workspace = true

[dependencies]
async-stream = "0.3.6"
bytes = "1.10.1"
log.workspace = true
postgres-types = "0.2.10"
rust_decimal = { workspace = true, features = ["db-tokio-postgres"] }
tank-core.workspace = true
time.workspace = true
tokio.workspace = true
tokio-postgres = { version = "0.7.14", features = ["with-time-0_3", "with-uuid-1"] }
url.workspace = true
urlencoding.workspace = true
uuid.workspace = true

[dev-dependencies]
tank-tests = { path = "../tank-tests", features = ["disable-maps", "disable-duration"] }
testcontainers-modules = { version = "0.13.0", features = ["postgres"] }
tokio.workspace = true
